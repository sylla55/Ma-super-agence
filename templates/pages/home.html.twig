{% extends 'base.html.twig' %}
{% block title 'Mon agence' %}

{% block body %}
    <div class="jumbotron">
        <h1>Mon agence</h1>
        <p>
            Lorem ipsum dolor sit amet consectetur adipisicing elit. Inventore, a expedita deleniti earum at architecto, ea facilis nisi ipsa et accusantium cupiditate numquam error neque corrupti aliquid? Nisi, iste alias.
        </p>
    </div>

    <div class="container">
        <h2 class="mt-4 text-center text-success">Les derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('property.show',{id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                            </h5>
                            <p class="card-text">
                                {{ property.address }}, {{ property.city }} {{ property.postalCode }} 
                            </p>
                            <div class="text-primary" style="font-size: 2rem; font-weight: bold;">
                                {{ property.FormattedPrice }}€
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
